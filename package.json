{
  "description": "this file only provides 'npm start' to run a web server with PUT support for updates. Smalltalk78 itself has no dependencies.",
  "scripts": {
    "start": "node devserver.js"
  },
  "devDependencies": {
    "express": "^4.19.2",
    "serve-index": "^1.9.1"
  }
}
